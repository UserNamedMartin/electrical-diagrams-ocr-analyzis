[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ocr"
version = "0.0.1"
requires-python = ">=3.11"

[project.scripts]
ocr = "ocr.app.cli:main"

[tool.setuptools.packages.find]
where = ["src"]